if (!tags.firstClick) {
    masks.firstClick = true
    os.hideTips()
    masks.onPointerDown = `@//done`
    masks.onPointerEnter = `@tags.color = '#e9ecef'
var tailBot = getBot(byTag("type", "speechBubbleTail"))
tailBot.tags.color = '#fafeff'`
}