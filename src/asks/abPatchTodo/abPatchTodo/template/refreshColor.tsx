// [Ryan] Don't change the color of the todo bots, only the arrows at the moment.
// tags.color = tags.todoBaseColor;

if (links.arrowBot) {
    setTag(links.arrowBot, 'color', tags.todoBaseColor);
}
