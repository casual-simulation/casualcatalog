const shard: ABArtifactShard = {
    data: {
        fillAmount: tags.fillAmount,
        fillColor: tags.fillColor,
    },
    dependencies: [
        {
            askID: 'rc-cup',
        }
    ]
}

return shard;