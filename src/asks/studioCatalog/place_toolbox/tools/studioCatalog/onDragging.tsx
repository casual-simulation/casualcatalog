if (links.defaultVisualBot) {
    links.defaultVisualBot.tags[that.to.dimension + 'X'] = that.to.x;
    links.defaultVisualBot.tags[that.to.dimension + 'Y'] = that.to.y;
}

tags[that.to.dimension + 'X'] = that.to.x;
tags[that.to.dimension + 'Y'] = that.to.y;