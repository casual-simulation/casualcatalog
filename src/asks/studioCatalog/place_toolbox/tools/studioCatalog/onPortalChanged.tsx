if (that.portal == 'gridPortal' || that.portal == 'mapPortal' || that.portal == 'systemPortal' || that.portal == 'sheetPortal') {
    ab.links.equipment.onEquipmentBaseDeselected(thisBot);
    thisBot.hideCatalog()
}